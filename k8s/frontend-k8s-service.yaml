#### k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: whatsapp-frontend-service
spec:
  selector:
    app: whatsapp-frontend
  ports:
    - port: 80
      targetPort: 8080
  type: NodePort
